# Welcome to Great Expectations! Always know what to expect from your data.
#
# Datasources tell Great Expectations where your data lives and how to get it.
# Read more at https://docs.greatexpectations.io/docs/guides/connecting_to_your_data/connect_to_data_overview

config_version: 3.0

# Use environment variables or config_variables.yml for credentials — never hardcode passwords.
# Example: connection_string: postgresql+psycopg2://${PG_USER}:${PG_PASSWORD}@${PG_HOST}:${PG_PORT}/${PG_DB}
config_variables_file_path: uncommitted/config_variables.yml

plugins_directory: plugins/

stores:
  expectations_store:
    class_name: ExpectationsStore
    store_backend:
      class_name: TupleFilesystemStoreBackend
      base_directory: expectations/

  validations_store:
    class_name: ValidationsStore
    store_backend:
      class_name: TupleFilesystemStoreBackend
      base_directory: uncommitted/validations/

  evaluation_parameter_store:
    class_name: EvaluationParameterStore

  checkpoint_store:
    class_name: CheckpointStore
    store_backend:
      class_name: TupleFilesystemStoreBackend
      suppress_store_backend_id: true
      base_directory: checkpoints/

  profiler_store:
    class_name: ProfilerStore
    store_backend:
      class_name: TupleFilesystemStoreBackend
      suppress_store_backend_id: true
      base_directory: profilers/

expectations_store_name: expectations_store
validations_store_name: validations_store
evaluation_parameter_store_name: evaluation_parameter_store
checkpoint_store_name: checkpoint_store

data_docs_sites:
  local_site:
    class_name: SiteBuilder
    show_how_to_buttons: true
    store_backend:
      class_name: TupleFilesystemStoreBackend
      base_directory: uncommitted/data_docs/local_site/
    site_index_builder:
      class_name: DefaultSiteIndexBuilder

anonymous_usage_statistics:
  data_context_id: 0cec2e07-9643-4782-9ba1-c4f622b7ea8f
  enabled: true

# --- Canonical Datasources (only 2 active sources) ---
fluent_datasources:
  # PostgreSQL datasource: staging.text_comment_1 (batch data from DB)
  postgres_staging:
    type: postgres
    assets:
      text_comment_1:
        type: table
        order_by: []
        batch_metadata: {}
        table_name: text_comment_1
        schema_name: staging
    connection_string: postgresql+psycopg2://${PG_USER}:${PG_PASSWORD}@${PG_HOST}:${PG_PORT}/${PG_DB}

  # Kafka/CSV datasource: streaming data dumped as CSV files
  kafka_filesystem:
    type: pandas_filesystem
    assets:
      streaming_csv:
        type: csv
        batching_regex: streaming\.csv
    base_directory: ../data/kafka

include_rendered_content:
  globally: false
  expectation_suite: false
  expectation_validation_result: false
