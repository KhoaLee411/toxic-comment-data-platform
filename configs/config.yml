datalake:
  endpoint: localhost:9000
  endpoint_docker: minio:9000
  bucket_name: raw
  folder_name: delta_lake
  access_key: ${MINIO_ROOT_USER}
  secret_key: ${MINIO_ROOT_PASSWORD}
  secure: false

dwh:
  host: localhost
  host_docker: postgres_datalake
  port: 5433
  port_docker: 5432
  database: ${POSTGRES_DB}
  user: ${POSTGRES_USER}
  password: ${POSTGRES_PASSWORD}
  staging_schema: staging
  production_schema: production
  stream_schema: stream

data:
  csv_path: ./data_local/raw
  delta_path: ./data_local/delta_format

spark:
  executor_memory: 8g

model:
  name: distilbert-base-uncased
  max_length: 512

stream:
  table: stream.raw_comments
  sleep_secs: 0.05
  kafka_bootstrap_servers: localhost:9092
  kafka_bootstrap_servers_docker: stream-broker:29092
  topic: toxic_comments.stream.raw_comments
