data_lake:
  endpoint: localhost:9000
  bucket_name: datalake
  folder_bronze: bronze
  folder_silver: silver
  folder_gold: gold
  access_key: ${MINIO_ROOT_USER}
  secret_key: ${MINIO_ROOT_PASSWORD}
  secure: false

data_warehouse:
  host: localhost
  port: 5433
  database: ${POSTGRES_DB}
  user: ${POSTGRES_USER}
  password: ${POSTGRES_PASSWORD}

raw: ./raw
